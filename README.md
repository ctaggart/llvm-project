# This repository has moved

`ctaggart/llvm-project` is no longer maintained. Development has moved to:

## 👉 [cataggar/llvm-project](https://github.com/cataggar/llvm-project)

Please update your remotes, bookmarks, and links accordingly:

```sh
git remote set-url origin https://github.com/cataggar/llvm-project.git
```

The new repository carries the same work, including the Zig-oriented prebuilt
LLVM toolchains (`llvm-zig-<version>`).
